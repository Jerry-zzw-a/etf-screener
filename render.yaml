services:
  - type: web
    name: etf-screener
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cloud_start.py
    plan: free
    healthCheckPath: /login
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
